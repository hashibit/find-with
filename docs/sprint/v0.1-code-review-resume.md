PRD 要求但 schema 没有：
  - projects——项目经历，PRD 明确列为独立实体
  - certifications——证书与奖项
  - workExperience.isRemote / workExperience.employmentType——远程/全职判断
  - education.isCurrentlyEnrolled——在读状态

  schema 设计本身的问题：
  - skills.kind 分 HARD/SOFT/TOOL 三类，但实际提取时 LLM 很难区分 HARD 和 TOOL（Python
  是 HARD 还是 TOOL？），容易产生噪音
  - basicInfo 缺 githubUrl、portfolioUrl——北美技术岗求职者几乎必填
  - workExperience 没有 isCurrent 字段，当前职位只能靠 end 是否为空推断，不可靠

  对 Quinn 最关键的缺失：
  projects 是素材库挖掘的核心来源，第一次深度档案对话很大程度会围绕项目展开。没有这个字  段，onboarding 阶段 Quinn 的能力会打折。

  建议至少把 projects 加上，其他可以 v0.2 补。
