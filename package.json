{
  "name": "findwith-monorepo",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:version": "prettier -v",
    "lint": "pnpm --recursive run lint",
    "proto:lint": "buf lint",
    "proto:breaking": "buf breaking --against .git#branch=main",
    "proto:generate": "buf generate",
    "ext:dev": "pnpm --filter extension dev",
    "ext:build": "pnpm --filter extension build",
    "ext:test": "pnpm --filter extension test",
    "web:dev": "pnpm --filter web dev",
    "web:build": "pnpm --filter web build",
    "web:test": "pnpm --filter web test",
    "test:infra:up": "docker compose -f docker-compose.test.yml up -d --wait",
    "test:infra:down": "docker compose -f docker-compose.test.yml down",
    "test:int": "pnpm test:infra:up && pnpm --filter backend-ts run test:int:migrate && pnpm --filter backend-ts run test:int",
    "e2e:up": "docker compose -f docker-compose.e2e.yml up -d",
    "e2e:down": "docker compose -f docker-compose.e2e.yml down -v",
    "e2e:migrate": "cd backend-ts && set -a && . .env.e2e && set +a && pnpm run migration:run",
    "e2e:seed": "cd backend-ts && set -a && . .env.e2e && set +a && tsx ../e2e/fixtures/seed.ts",
    "build:backend": "pnpm --filter @findwith/backend run build",
    "build:extension:e2e": "bash e2e/scripts/build-extension-e2e.sh",
    "e2e": "NODE_OPTIONS=--no-strip-types playwright test",
    "e2e:record": "LLM_RECORD=true NODE_OPTIONS=--no-strip-types playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "prettier": "^3.3.3"
  },
  "pnpm": {
    "overrides": {
      "@tiptap/extension-horizontal-rule": "2.1.13"
    },
    "patchedDependencies": {
      "@adminjs/nestjs@7.0.0": "patches/@adminjs__nestjs@7.0.0.patch"
    }
  }
}
